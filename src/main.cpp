#include <iostream>
#include <ctime>
#include <bits/stdc++.h>
#include "all-headers.h"
using namespace std;

int main() {
	srand(time(0));
	// Lists list = new Tlists();
	// function_call(list);
	FVector v = init_fvector("v1");
	print_fvector(v);
	// FMatrix m = init_fmatrix("B");
	// print_fmatrix(m);
	// FMatrix mr = fraction_matrix_rref(m);
	// print_fmatrix(mr);

	return 0;
}
